apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: sabby404/demo-docker-repository:tagversion
      ports:
        - containerPort: 8080
